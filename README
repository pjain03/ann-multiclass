DESCRIPTION:
Neural Network implementations to classify data into multiple classes 
(2 or more). We take a dataset and the number of classes to classify into, 
process the dataset into hot-encoded labels.

DATASETS:
1. Iris (https://archive.ics.uci.edu/ml/machine-learning-databases/iris/)

PACKAGES:
pickle

INSTRUCTIONS:
1. pip install -r requirements.txt
2. python scripts/process.py